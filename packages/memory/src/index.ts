// Memory Service (blueprint §7) — built in M3. M0 pins the contract only:
// typed CRUD over memory_records with mandatory provenance, confidence decay,
// and supersession (never silent overwrite). See MemoryRecord in @ai-os/shared.
export const MILESTONE = 'M3';
